apiVersion: v2
name: quick-links
description: The goto.*/qr.* short-link + secure-redirect service, its Postgres, and pgAdmin — one deployable unit.
type: application
version: 0.1.0
appVersion: "0.1.0"
