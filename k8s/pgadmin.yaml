---
# pgAdmin entrypoint script as ConfigMap (creates pgpass + servers.json from env vars)
apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin-entrypoint
data:
  entrypoint.sh: |
    #!/bin/sh
    export PGADMIN_CONFIG_SERVER_MODE=True
    export PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED=True

    SERVERS_JSON_PATH="/pgadmin4/servers.json"
    PGPASSFILE="$HOME/.pgpass"

    echo "${POSTGRES_HOST}:*:*:${POSTGRES_USER}:${POSTGRES_PASSWORD}" > "$PGPASSFILE"
    chmod 600 $PGPASSFILE

    cat << EOF > $SERVERS_JSON_PATH
    {
        "Servers": {
            "1": {
                "Name": "Primary Database",
                "Group": "Servers",
                "Host": "${POSTGRES_HOST}",
                "Port": 5432,
                "MaintenanceDB": "${POSTGRES_DB}",
                "Username": "${POSTGRES_USER}",
                "PassFile": "$PGPASSFILE",
                "SSLMode": "prefer"
            }
        }
    }
    EOF

    exec /entrypoint.sh
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quicklinks-pgadmin-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quicklinks-pgadmin
  labels:
    app: quicklinks-pgadmin
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: quicklinks-pgadmin
  template:
    metadata:
      labels:
        app: quicklinks-pgadmin
    spec:
      securityContext:
        runAsUser: 0
      # pgadmin is an admin tool — runs on the server node (where PVC is bound)
      # No untrusted toleration needed.
      containers:
        - name: pgadmin
          image: dpage/pgadmin4:8
          ports:
            - containerPort: 80
          env:
            - name: POSTGRES_HOST
              value: db
          envFrom:
            - secretRef:
                name: quicklinks-postgres
          command: ["/bin/sh", "/entrypoint_custom.sh"]
          volumeMounts:
            - name: data
              mountPath: /var/lib/pgadmin
            - name: entrypoint
              mountPath: /entrypoint_custom.sh
              subPath: entrypoint.sh
          resources:
            requests:
              cpu: "20m"
              memory: "64Mi"
            limits:
              memory: "256Mi"
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: quicklinks-pgadmin-data
        - name: entrypoint
          configMap:
            name: pgadmin-entrypoint
            defaultMode: 0755
---
apiVersion: v1
kind: Service
metadata:
  name: quicklinks-pgadmin
spec:
  type: ClusterIP
  selector:
    app: quicklinks-pgadmin
  ports:
    - port: 80
      targetPort: 80
---
# Tailscale-only ingress — Caddy tailscale-proxy forwards here
# cert-manager DNS-01 via Cloudflare works even for private/TS-only domains
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: quicklinks-pgadmin
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-cloudflare
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - pgadmin.ts.conrad-klaus.de
      secretName: pgadmin-tls
  rules:
    - host: pgadmin.ts.conrad-klaus.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: quicklinks-pgadmin
                port:
                  number: 80
